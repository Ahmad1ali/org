{% extends 'base.html.twig' %}


{% block body %}
    {% for product in products%}
        <h1>

        </h1>

        <p>{{ product.name }}</p>
{#        <img src={{ asset( product.image) }}>#}
        <p>{{ product.desciption }}</p>

    {% endfor %}

{% endblock %}